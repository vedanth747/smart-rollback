apiVersion: v1
kind: Service
metadata:
  name: rollback-app
  labels:
    app: rollback-app
spec:
  type: NodePort
  selector:
    app: rollback-app
  ports:
    - name: http
      port: 3000
      targetPort: 3000
